# shared-products

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-products` to build the library.

## Running unit tests

Run `nx test shared-products` to execute the unit tests via [Jest](https://jestjs.io).
